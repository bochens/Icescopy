\documentclass[
  9pt,
  letterpaper,
  oneside,
  open=any,
  DIV=15,
  parskip=half-,
  headings=big,
  numbers=noenddot
]{scrbook}

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[final]{microtype}
\usepackage[dvipsnames,svgnames,x11names]{xcolor}
\usepackage{graphicx}
\usepackage{bookmark}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage[most]{tcolorbox}
\usepackage{fvextra}
\usepackage{scrlayer-scrpage}
\usepackage{setspace}

\definecolor{ManualInk}{HTML}{1C1E21}
\definecolor{ManualMuted}{HTML}{596273}
\definecolor{ManualBlue}{HTML}{2457D6}
\definecolor{ManualBlueSoft}{HTML}{EEF3FF}
\definecolor{ManualBorder}{HTML}{C9D5F2}
\definecolor{ManualCodeBg}{HTML}{F6F8FB}

\hypersetup{
  colorlinks=true,
  linkcolor=ManualBlue,
  urlcolor=ManualBlue,
  pdftitle={Troubleshoot Common Problems},
  pdfauthor={OpenAI Codex},
  pdfsubject={Icescopy software manual}
}

\setcounter{tocdepth}{1}
\setcounter{secnumdepth}{2}

\setstretch{0.97}
\setlength{\parskip}{0.18em}
\setlength{\parindent}{0pt}
\setlist[itemize]{leftmargin=*, itemsep=0.08em, topsep=0.08em, parsep=0pt, partopsep=0pt}
\setlist[enumerate]{leftmargin=*, itemsep=0.08em, topsep=0.08em, parsep=0pt, partopsep=0pt}

\addtokomafont{disposition}{\sffamily\bfseries\color{ManualInk}}
\addtokomafont{chapter}{\fontsize{18}{20}\selectfont}
\addtokomafont{section}{\fontsize{12.2}{14}\selectfont}
\addtokomafont{subsection}{\fontsize{10.8}{12.2}\selectfont}
\addtokomafont{pagehead}{\sffamily\small\color{ManualMuted}}
\addtokomafont{pagenumber}{\sffamily\small\color{ManualMuted}}

\renewcommand*{\chapterheadstartvskip}{\vspace*{-1.8\baselineskip}}
\renewcommand*{\chapterheadendvskip}{\vspace{0.8\baselineskip}}

\clearpairofpagestyles
\automark[section]{chapter}
\ihead{\headmark}
\ohead{\pagemark}
\KOMAoptions{headsepline=true}

\newcommand{\manuallead}[1]{%
  \vspace{0.15em}%
  {\sffamily\bfseries\color{ManualBlue}#1}\par\nobreak\vspace{0.05em}%
}

\newenvironment{manualcode}
  {\VerbatimEnvironment
   \begin{tcolorbox}[
     breakable,
     enhanced,
     sharp corners,
     colback=ManualCodeBg,
     colframe=ManualBorder,
     boxrule=0.4pt,
     left=0.9mm,
     right=0.9mm,
     top=0.5mm,
     bottom=0.5mm,
     arc=0.8mm
   ]
   \begin{Verbatim}[fontsize=\footnotesize,breaklines=true,breakanywhere=true]}
  {\end{Verbatim}\end{tcolorbox}}

\begin{document}

\begin{titlepage}
  \thispagestyle{empty}
  \centering
  \vspace*{1.1cm}
  \includegraphics[width=1.8cm]{IcescopyApp.png}\par
  \vspace{0.75cm}
  {\sffamily\bfseries\fontsize{24}{27}\selectfont Troubleshoot Common Problems\par}
  \vspace{0.25cm}
  {\sffamily\normalsize\color{ManualMuted}Icescopy manual chapter\par}
  \vspace{0.8cm}
  \begin{tcolorbox}[
    width=0.84\textwidth,
    enhanced,
    colback=ManualBlueSoft,
    colframe=ManualBorder,
    boxrule=0.5pt,
    arc=1.4mm,
    left=2.5mm,
    right=2.5mm,
    top=2mm,
    bottom=2mm
  ]
    {\sffamily\bfseries\color{ManualInk}Product manual}\par
    \vspace{0.1em}
    {\color{ManualMuted}Current user documentation for the Icescopy desktop application.}
  \end{tcolorbox}
  \vfill
  {\sffamily\small\color{ManualMuted}Generated 2026-04-20}
\end{titlepage}

\frontmatter




\mainmatter
\chapter{Troubleshoot Common Problems}

Use this page to resolve the most common session, packaging, and import problems.


\section{Session Opens But Images Are Missing}

\manuallead{Symptom}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item the session opens
\item the image list is present
\item the images do not resolve
\end{itemize}

\manuallead{Action}

\begin{enumerate}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item Open \texttt{File -> Relink Images Folder...}.
\item Select the folder that contains the session images.
\end{enumerate}

\section{A Session Was Shared With Another Computer}

\manuallead{Cause}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item the saved absolute image paths do not exist on the second machine
\end{itemize}

\manuallead{Action}

\begin{enumerate}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item Open the session.
\item Relink the image folder on the new machine.
\end{enumerate}

\section{The Built App Does Not Launch}

\manuallead{Cause}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item you are running the intermediate PyInstaller \texttt{build/} output instead of the packaged app
\end{itemize}

\manuallead{Do not run}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item \texttt{build/Icescopy/Icescopy}
\end{itemize}

\manuallead{Run one of these instead}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item \texttt{dist/Icescopy.app}
\item \texttt{dist/Icescopy.app/Contents/MacOS/Icescopy}
\item \texttt{dist/Icescopy/Icescopy}
\end{itemize}

\section{Packaging Fails With Cache Or Permission Errors}

Use temporary cache locations during build:


\begin{manualcode}
PYINSTALLER_CONFIG_DIR=/tmp/icescopy-pyinstaller \
MPLCONFIGDIR=/tmp/icescopy-mpl \
XDG_CACHE_HOME=/tmp \
python -m PyInstaller --clean --noconfirm Icescopy.spec
\end{manualcode}

\section{Temperature Import Looks Wrong}

\manuallead{Check these first}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item the correct importer was used
\item sample names match where required
\item the reset temperature is appropriate for the run
\item cycle warm-up hysteresis is not too small
\item current freeze annotations are correct
\end{itemize}

\manuallead{Remember}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item CSU uses the \texttt{.dat} file as the authoritative picture-to-time mapping
\item TAMU uses filename time plus interpolation from the Linkam timeseries
\end{itemize}

\manuallead{Also check}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item whether sample names were finalized before CSU import
\item whether blank samples were selected only for CSU
\item whether the current freeze frames are correct before import
\end{itemize}

\section{A New Cycle Appears At The Wrong Time}

\manuallead{Cause}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item reset detection depends on both \texttt{Reset After Warmed To (°C)} and \texttt{Cycle Warm-Up Hysteresis (°C)}
\end{itemize}

\manuallead{Action}

\begin{enumerate}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item Review the chosen reset temperature.
\item Increase cycle warm-up hysteresis if minor temperature jitter is creating false cycle boundaries.
\end{enumerate}

\section{Result Tables Look Stale}

\manuallead{Cause}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item the session state changed after the table was generated
\end{itemize}

\manuallead{Action}

\begin{enumerate}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item Re-run analysis after changing cells, freeze annotations, samples, or image-edit settings.
\item Re-run temperature import after changing freeze annotations or sample assignments.
\end{enumerate}

\manuallead{Notes}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item \texttt{Cell Edit} and \texttt{Image Edit} can preserve existing tables in the session state
\item preserved tables are not automatically recomputed
\item image add, image removal, and image reorder operations invalidate analysis results
\end{itemize}

\section{Cells Drift Off The Droplets During The Run}

\manuallead{Cause}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item the geometry changes across frames but only one static cell layout is defined
\end{itemize}

\manuallead{Action}

\begin{enumerate}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item Add keyframes at frames where the layout visibly changes.
\item Edit cell geometry on those keyframes.
\item Scrub between keyframes and confirm the interpolation is accurate.
\item Re-run analysis after the geometry is corrected.
\end{enumerate}

\section{Freeze Finding Misses Real Events Or Calls Too Many}

\manuallead{Cause}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item the detector parameters do not match the signal shape in the current data
\end{itemize}

\manuallead{Action}

\begin{enumerate}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item Use the \texttt{Grayscale Plot} instead of guessing from the tables alone.
\item Compare the solid grayscale timeseries and dashed convolution timeseries against the image sequence.
\item Adjust \texttt{Peak Prominence} and \texttt{Peak Width} first.
\item Adjust convolution parameters only after the basic event thresholding is close.
\end{enumerate}

\section{The Packaged App Is Large}

\manuallead{Cause}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item the application bundles PySide6, OpenCV, NumPy, Pandas, SciPy, and PyQtGraph
\end{itemize}

\manuallead{Result}

\begin{itemize}[leftmargin=*,itemsep=0.35em,topsep=0.4em]
\item a large bundle size is expected unless dependencies are reduced
\end{itemize}

\end{document}
